name: kinya_learn
description: A Flutter app for learning Kinyarwanda language.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.0.5
  url_launcher: ^6.2.4
  video_player: ^2.8.1
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.2
  webview_flutter: ^4.4.4
  flutter_inappwebview: ^6.0.0
  
  # Firebase dependencies for CRUD operations
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  firebase_auth: ^4.15.3
  firebase_storage: ^11.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/images/greetings/

# Enable platforms
platforms:
  android:
  ios:
  web:
  windows:
  macos:
  linux:
